# Step 9｜自動テストの導入 - 学習まとめ

```
┌───────────────────────────────────────────┐
│  学んだこと                                  │
│  ① なぜテストを書くか                          │
│  ② Jest + jsdom + jQueryのテスト環境構築       │
│  ③ ロジック関数をテストしやすくする工夫           │
│  ④ 実際にテストを書く                          │
└───────────────────────────────────────────┘
```

`買い物リスト/`アプリのロジック（`addItem`/`toggleHeld`/`deleteStore`等）に対して、Jestによる自動テストを導入した記録。CIへの組み込みやブランチ保護ルールとの連携はGitHub固有の話のため`Step8_GitHub連携.md`の14章にまとめており、本ドキュメントは「テストの書き方」そのものに絞っている。

---

## 1. なぜテストを書くか

これまでは、機能を実装するたびに`npx eslint`で構文チェックをし、ブラウザを開いて手動で動作確認していた。ESLintは「構文が正しいか」しか見てくれず、「ロジックが意図通りに動くか」（例: 数量1のときに－を押すと削除される、保留にすると購入が自動解除される、など）は毎回手で確認する必要があった。

自動テストを書くと、こうした「振る舞いの正しさ」をコードとして固定できる。以後、別の変更でロジックを壊してしまっても、テストが落ちることで気づける。

## 2. Jest + jsdom + jQueryのテスト環境構築

**Node.jsとは**: ブラウザの外でJavaScriptを実行できるようにする実行環境（JavaScriptランタイム）。元々JavaScriptはブラウザの中でしか動かない言語だったが、ChromeのJavaScriptエンジン「V8」を取り出し、ブラウザなしでも動くようにしたのがNode.js。これにより、サーバーサイドの処理やCLIツール、ビルドツール、そして今回のようなテストの実行もJavaScriptで書けるようになった。`npm`（Node Package Manager）はNode.jsに付属するパッケージ管理ツールで、ESLintやJestなどの外部ツールをインストールして使うための仕組み。`npx eslint ...`や`npm test`はすべてNode.js上で動くコマンド。

ブラウザとの大きな違いは、ブラウザには`document`/`window`などDOM関連APIがある一方、Node.jsにはそれらが存在しないこと（代わりにファイル読み書きなどOSに近い操作ができる）。今回`app.js`をNode.js（Jest）でテストする際に「`document`がない」問題が起きたのは、まさにこの違いによるもので、後述のjsdomで解決している。

**Jestとは**: Meta（旧Facebook）が開発したJavaScript用のテストフレームワーク。`test()`（や`it()`）でテストケースを書き、`expect(実際の値).toBe(期待する値)`のような形で検証する。`npm test`（実体は`jest`コマンド）を実行すると、対象ファイルを自動で探して全テストを実行し、pass/failを一覧表示する。モックやスナップショットテストなど、テストに必要な機能が最初から一通り揃っている「オールインワン」型のツールで、追加ライブラリなしで動く。

似たツールとして、Jestとほぼ同じ書き方ができるより高速な後発ツール**Vitest**（Viteベースのプロジェクトでよく使われる）や、古くからある**Mocha + Chai**の組み合わせ（Mochaはアサーション部分を別ライブラリと組み合わせる構成）がある。

**jsdomとは**: JavaScriptだけでブラウザのDOM（`document`/`window`や`document.getElementById`など）を再現するライブラリ。Node.jsには本来ブラウザがないため、`document`や`localStorage`のようなブラウザ専用のAPIは存在しない。jsdomを使うと、Node.js上に「疑似的なブラウザ環境」を用意でき、ブラウザ向けに書かれたコード（今回の`app.js`のようなjQueryコード）をそのままNode.js上で動かして検証できる。Jestは`testEnvironment: "jsdom"`と設定するだけでこの環境を自動的に用意してくれる（実体は`jest-environment-jsdom`というパッケージ）。

`買い物リスト/js/app.js`はビルドツールなしでブラウザの`<script>`タグから直接読み込まれる、素のjQuery 1.11コード。モジュールシステム（`import`/`export`）を使っていないため、そのままではNode.js（Jest）から関数を読み込めない。

**対応した点**:

| 課題 | 対応 |
|---|---|
| ブラウザは`module`を持たないが、Node/Jestは持つ | `app.js`末尾に`if (typeof module !== "undefined" && module.exports) { module.exports = {...}; }`を追加。ブラウザでは`module`が存在しないためこのブロックは実行されず、既存の動作に影響しない |
| `app.js`内で素の`$(...)`を直接呼んでいる | `tests/jest.setup.js`で`global.$ = global.jQuery = require("jquery");`しておき、`app.js`をrequireしたときに参照できるようにする |
| `app.js`が`Sortable.create(...)`を呼んでいる（CDN経由の外部ライブラリ） | 同じく`jest.setup.js`で`global.Sortable = { create: function () {} };`というダミーを用意し、requireしただけでエラーにならないようにする |
| `package.json`の設定 | `"scripts": { "test": "jest" }`、`"jest": { "testEnvironment": "jsdom", "setupFiles": ["<rootDir>/tests/jest.setup.js"] }`を追加 |

**ポイント**: `typeof module !== "undefined"`のようなガード付きエクスポートは、モジュールシステムを持たない既存の「素のスクリプト」コードに後からテストを追加する際の定番パターン。書き方を大きく変えずに済む。

## 3. ロジック関数をテストしやすくする工夫

`addItem`や`toggleHeld`のような関数は、データの更新（`localStorage`への保存）と画面の再描画（`renderList`によるDOM操作）の両方を行っている。今回はDOM描画側の検証はせず、「関数を呼んだ後、`loadItems()`で読み返した状態が期待通りか」だけを確認する方針にした。

```
テストの基本形:
  1. app.addItem(...) のようにロジック関数を呼ぶ
  2. app.loadItems() / app.loadStores() で保存後の状態を読み出す
  3. 期待する値になっているかをexpectで確認する
```

DOM描画（jQueryでの要素生成やSortableJSの見た目）まで含めた検証はしていない。ロジックの正しさとDOM描画の正しさを分けて考え、まずは検証しやすい「データの状態」だけをテスト対象にした。

## 4. 実際に書いたテスト

`tests/app.test.js`に7件のテストを書いた。

| テスト内容 | 確認していること |
|---|---|
| `addItem`が前後の空白を除去して追加する | 入力値のトリム処理 |
| `addItem`が空文字を追加しない | 空文字ガード |
| `addItem`が数量が不正なら1にする | `parseInt`失敗時のフォールバック |
| `toggleHeld`で保留にすると購入は自動解除される | 保留と購入が同時に成立しない、という業務ルール |
| `decreaseQuantity`で数量1のときに押すと削除される | 数量0にはせず削除に切り替わる境界値 |
| `deleteStore`で店舗を消すと商品は未設定に戻る | 店舗削除時のデータ整合性 |
| `clearAllItems`で商品と店舗が両方消える | 全件削除の副作用（店舗も消える） |

**学び**: どれも「境界値」や「複数の状態が絡む業務ルール」を優先的にテストした。単純な代入だけの処理より、条件分岐や副作用がある処理の方がバグが混入しやすく、テストの効果が高い。

**補足: テストの3つのパターン**

7件のテストは、大きく3種類に分類できる。「関数に正常な引数/異常な引数を渡して結果を見る」という単純な入出力テストだけではなく、状態の変化そのものを検証しているテストも多い。

```
① 入力値のバリデーション（正常系・異常系の引数）
   例: addItem に "  牛乳  "（前後空白あり）→ トリムされて追加される
       addItem に "   "（空文字）        → 追加されない
       addItem に "abc"（不正な数量）     → 数量1にフォールバック

② 状態遷移・業務ルール（複数のフラグ・データが絡む挙動）
   例: toggleHeld で保留にする → purchased が自動でfalseに戻る
       deleteStore で店舗を削除 → その店の商品のstoreが""に戻る
       clearAllItems を呼ぶ    → items と stores の両方が消える

③ 境界値（データの状態が特定の境目のときの挙動）
   例: decreaseQuantity で quantity=1 のときに押す → 削除に切り替わる
       （0にはしない、という境界条件）
```

①に当てはまるのは`addItem`関連の3件のみで、`toggleHeld`/`decreaseQuantity`/`deleteStore`/`clearAllItems`の4件はどれも**引数自体は正常**であり、「その関数を呼んだ結果、他のデータや状態がどう変わるか（副作用・業務ルール）」を確認している。引数の正常系/異常系だけでなく、関数の性質に応じてこの3パターンを使い分けることが、意味のあるテストを書くうえでの視点になる。

**補足: テスト対象は関数の一部のみ（網羅はしていない）**

`app.js`には現在17個の関数があるが、テストを書いたのはそのうち5個（`addItem`・`toggleHeld`・`decreaseQuantity`・`deleteStore`・`clearAllItems`）だけ。残り12個は今回テスト対象外にした。

| 未テストの関数 | 対象外にした理由 |
|---|---|
| `loadItems`/`saveItems`/`loadStores`/`saveStores` | localStorageの読み書きだけの単純な処理。テスト済みの他関数を通じて間接的に動作確認できている |
| `renderList` | DOM描画そのもの。「ロジックの正しさ」と「DOM描画の正しさ」を分けて考え、今回は意図的にテスト対象外とした |
| `toggleItem`/`increaseQuantity`/`deleteItem`/`editItemName`/`addStore`/`reorderItems`/`reorderStores` | 条件分岐や副作用の構造が、既にテストした`toggleHeld`や`decreaseQuantity`などと似ている。全網羅ではなく、境界値・業務ルール・入力検証が絡む箇所を優先した結果、代表的なものに絞った |

自動テストは「全部の関数を必ずテストする」ものではなく、「バグが混入しやすい箇所（条件分岐・副作用・境界値がある箇所）を優先してカバーする」という考え方で書いてよい。

**補足: テストしていない関数の品質はどう担保するか**

```
① 手動のブラウザ確認（毎回の実装フローに組み込み済み）
② ESLint（構文・undefined変数のチェック）
③ 間接的なカバレッジ（他関数のテスト経由で動く）
④ 関数のシンプルさ（複雑度が低いほどバグりにくい）
```

- **①手動のブラウザ確認**: 「実装 → ESLint確認 → ブラウザで動作確認 → コミット」という一連の流れそのものが実質的な動作確認になっている。`renderList`（DOM描画）・`toggleItem`（購入チェック）・`increaseQuantity`/`deleteItem`（数量・削除）・`editItemName`（編集）・`addStore`（店舗追加）はすべて、追加した直後に実際に画面で操作して確認済み。ただしこれは「その場限りの確認」であり、後から別の変更で壊れても自動では気づけない（リグレッション防止にはならない）という限界がある
- **②ESLint**: 未定義変数の参照や構文ミスなど、「明らかな間違い」はテストがなくても機械的に検出される
- **③間接的なカバレッジ**: `addItem`のテストは内部で`loadItems`/`saveItems`を呼んでいるため、単体テストこそないがこれらは他のテストを通して間接的に動作確認されている
- **④複雑度の低さ**: `loadItems`/`saveItems`はlocalStorageの読み書きだけの数行の処理で、条件分岐がほとんどない。バグの入り込む余地が構造的に少ない

**正直な限界**: `renderList`のようなDOM描画部分は、今の構成（Jest+jsdomでロジックだけをテスト）では自動テストの対象にできていない。DOM描画まで自動でテストしたい場合は、Testing Library（DOM要素の存在や見た目を検証するツール）やPlaywright/Cypressのようなブラウザ自動操作ツール（E2Eテスト）を別途導入する必要があり、これは今回のスコープ外の発展課題として残っている。

## チェックポイント

- [x] なぜ自動テストが必要かを、手動確認との違いで説明できる
- [x] モジュールシステムを持たない既存コードに、ブラウザ動作を壊さずテスト用のエクスポートを追加できる
- [x] Jest + jsdom + jQueryでレガシーなjQueryコードのテスト環境を構築できる
- [x] 境界値・業務ルールを優先してテストケースを書ける
