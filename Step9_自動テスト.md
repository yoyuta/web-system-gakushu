# Step 9｜自動テストの導入 - 学習まとめ

```
┌───────────────────────────────────────────┐
│  学んだこと                                  │
│  ① なぜテストを書くか                          │
│  ② Jest + jsdom + jQueryのテスト環境構築       │
│  ③ ロジック関数をテストしやすくする工夫           │
│  ④ 実際にテストを書く                          │
└───────────────────────────────────────────┘
```

`買い物リスト/`アプリのロジック（`addItem`/`toggleHeld`/`deleteStore`等）に対して、Jestによる自動テストを導入した記録。CIへの組み込みやブランチ保護ルールとの連携はGitHub固有の話のため`Step8_GitHub連携.md`の14章にまとめており、本ドキュメントは「テストの書き方」そのものに絞っている。

---

## 1. なぜテストを書くか

これまでは、機能を実装するたびに`npx eslint`で構文チェックをし、ブラウザを開いて手動で動作確認していた。ESLintは「構文が正しいか」しか見てくれず、「ロジックが意図通りに動くか」（例: 数量1のときに－を押すと削除される、保留にすると購入が自動解除される、など）は毎回手で確認する必要があった。

自動テストを書くと、こうした「振る舞いの正しさ」をコードとして固定できる。以後、別の変更でロジックを壊してしまっても、テストが落ちることで気づける。

## 2. Jest + jsdom + jQueryのテスト環境構築

**Jestとは**: Meta（旧Facebook）が開発したJavaScript用のテストフレームワーク。`test()`（や`it()`）でテストケースを書き、`expect(実際の値).toBe(期待する値)`のような形で検証する。`npm test`（実体は`jest`コマンド）を実行すると、対象ファイルを自動で探して全テストを実行し、pass/failを一覧表示する。モックやスナップショットテストなど、テストに必要な機能が最初から一通り揃っている「オールインワン」型のツールで、追加ライブラリなしで動く。

似たツールとして、Jestとほぼ同じ書き方ができるより高速な後発ツール**Vitest**（Viteベースのプロジェクトでよく使われる）や、古くからある**Mocha + Chai**の組み合わせ（Mochaはアサーション部分を別ライブラリと組み合わせる構成）がある。

`買い物リスト/js/app.js`はビルドツールなしでブラウザの`<script>`タグから直接読み込まれる、素のjQuery 1.11コード。モジュールシステム（`import`/`export`）を使っていないため、そのままではNode.js（Jest）から関数を読み込めない。

**対応した点**:

| 課題 | 対応 |
|---|---|
| ブラウザは`module`を持たないが、Node/Jestは持つ | `app.js`末尾に`if (typeof module !== "undefined" && module.exports) { module.exports = {...}; }`を追加。ブラウザでは`module`が存在しないためこのブロックは実行されず、既存の動作に影響しない |
| `app.js`内で素の`$(...)`を直接呼んでいる | `tests/jest.setup.js`で`global.$ = global.jQuery = require("jquery");`しておき、`app.js`をrequireしたときに参照できるようにする |
| `app.js`が`Sortable.create(...)`を呼んでいる（CDN経由の外部ライブラリ） | 同じく`jest.setup.js`で`global.Sortable = { create: function () {} };`というダミーを用意し、requireしただけでエラーにならないようにする |
| `package.json`の設定 | `"scripts": { "test": "jest" }`、`"jest": { "testEnvironment": "jsdom", "setupFiles": ["<rootDir>/tests/jest.setup.js"] }`を追加 |

**ポイント**: `typeof module !== "undefined"`のようなガード付きエクスポートは、モジュールシステムを持たない既存の「素のスクリプト」コードに後からテストを追加する際の定番パターン。書き方を大きく変えずに済む。

## 3. ロジック関数をテストしやすくする工夫

`addItem`や`toggleHeld`のような関数は、データの更新（`localStorage`への保存）と画面の再描画（`renderList`によるDOM操作）の両方を行っている。今回はDOM描画側の検証はせず、「関数を呼んだ後、`loadItems()`で読み返した状態が期待通りか」だけを確認する方針にした。

```
テストの基本形:
  1. app.addItem(...) のようにロジック関数を呼ぶ
  2. app.loadItems() / app.loadStores() で保存後の状態を読み出す
  3. 期待する値になっているかをexpectで確認する
```

DOM描画（jQueryでの要素生成やSortableJSの見た目）まで含めた検証はしていない。ロジックの正しさとDOM描画の正しさを分けて考え、まずは検証しやすい「データの状態」だけをテスト対象にした。

## 4. 実際に書いたテスト

`tests/app.test.js`に7件のテストを書いた。

| テスト内容 | 確認していること |
|---|---|
| `addItem`が前後の空白を除去して追加する | 入力値のトリム処理 |
| `addItem`が空文字を追加しない | 空文字ガード |
| `addItem`が数量が不正なら1にする | `parseInt`失敗時のフォールバック |
| `toggleHeld`で保留にすると購入は自動解除される | 保留と購入が同時に成立しない、という業務ルール |
| `decreaseQuantity`で数量1のときに押すと削除される | 数量0にはせず削除に切り替わる境界値 |
| `deleteStore`で店舗を消すと商品は未設定に戻る | 店舗削除時のデータ整合性 |
| `clearAllItems`で商品と店舗が両方消える | 全件削除の副作用（店舗も消える） |

**学び**: どれも「境界値」や「複数の状態が絡む業務ルール」を優先的にテストした。単純な代入だけの処理より、条件分岐や副作用がある処理の方がバグが混入しやすく、テストの効果が高い。

## チェックポイント

- [x] なぜ自動テストが必要かを、手動確認との違いで説明できる
- [x] モジュールシステムを持たない既存コードに、ブラウザ動作を壊さずテスト用のエクスポートを追加できる
- [x] Jest + jsdom + jQueryでレガシーなjQueryコードのテスト環境を構築できる
- [x] 境界値・業務ルールを優先してテストケースを書ける
